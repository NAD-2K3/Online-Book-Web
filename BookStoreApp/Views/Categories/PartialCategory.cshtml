<div class="list-group">

    @foreach (var item in ViewBag.CategoryList)
    {
    <a href="@Url.Action("GetBookByCategoryID/" + item.ID_BookCategory,"Categories")" class="list-group-item list-group-item-action">@item.Name_BookCategory</a>

    }
</div>
